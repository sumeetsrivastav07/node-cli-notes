# Node CLI Notes App

A simple command-line notes manager built using **Node.js ES Modules**.
This project focuses on understanding the **Node runtime**, CLI input handling,
file-based persistence, and clean modular architecture — without using any frameworks.

---

## Features

- Add notes from the command line
- List all saved notes
- Persistent storage using a JSON file
- Input validation and clean CLI messages
- Modular, one-directional architecture

---

## Commands

### Add a note
```bash
node index.js add "Learn Node fundamentals"
node index.js list